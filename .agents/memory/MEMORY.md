- [Drizzle pg array parameter](drizzle-pg-array.md) — JS array in sql`...` → cast ::text[] fails; fix: ARRAY[${sql.join(items.map(i=>sql`${i}`), sql`, `)}]
- [CM Expert TO brand IDs](cm-to-brand-ids.md) — Haval=220, Jetour=4061303889, Soueast=2712535252; prod DB имена JETOUR/SOUEAST; VPS нужен CM_EXPERT_CLIENT_ID (9 символов, mb-...) — НЕ CMEXPERT_CLIENT_ID (11 символов, другой API)
- [VPS production stack](vps-production-stack.md) — полный переезд на VPS: PM2+bash wrapper+.env, nginx proxy→localhost:8080, API 3.7MB esbuild bundle, symlinks для pino workers, deploy via scripts/deploy-vps.sh.
- [OpenAI esbuild external](openai-esbuild-external.md) — только openai MUST be external; v6 SDK uses dynamic require("node:events") → crash if bundled.
- [VPS deploy correct path](vps-deploy-correct-path.md) — deploy to /opt/debryansk/api/ (not dist/); @workspace/integrations-openai-ai-server must be BUNDLED; prerender disk delete needs server restart.
- [Prerender manual rebuild](prerender-manual-rebuild.md) — brand pages have NO auto-prerender-on-miss; clearing cache → SPA shell forever; rebuild manually via prerender.mjs --route.
- [VPS deploy rules & script](vps-deploy-rules.md) — стандартизированный deploy: tar+sshpass, --strip-components вручную, **CRITICAL: всегда rebuild перед деплоем (никогда не доверять старому dist/), hash comparison для проверки свежести, backup, post-transfer verify, health check. Скрипт: scripts/deploy-vps.sh. **НЕ делать ручной tar extract в /opt/debryansk/api/ — сносит node_modules; openai external-пакет там живёт, без него PM2 падает с ERR_MODULE_NOT_FOUND.**
- [Timeweb VPS reverse proxy](timeweb-vps.md) — IP 5.42.110.134, SSH через `VPS_SSH_PASSWORD` secret + sshpass, Nginx→Replit (34.111.179.208), ssl_buffer_size 4k обязателен.
- [Vite proxy for API](vite-api-proxy.md) — Replit multi-artifact proxy chain: browser→port 8081 (Replit infra)→port 19052 (Vite)→proxy to port 8080 (API server). Port 8081 is NOT the main router.
- [API route prefix pitfall](api-route-prefix.md) — app.use("/api", router) in app.ts: router paths must NOT include /api prefix (write "/send-email", not "/api/send-email"). Also: multer/busboy must be in esbuild externals list alongside nodemailer.
- [Image optimization strategy](image-optimization.md) — WebP-first approach: all PNG/JPG converted to WebP (~95% size reduction), mobile variants created for hero, lazy loading + async decoding for all non-critical images, `<picture>` with srcset for responsive hero.
- [SVG optimization](svg-optimization.md) — Manual SVG cleanup: remove unused namespaces, redundant attributes, use `<g>` with `fill` instead of per-path fills, short gradient IDs. Jetour/VW/Haval-official SVGs remain as-is since they have unique source-embedded features.
- [GitHub API upload](github-api-upload.md) — When git push is blocked in main agent, use GitHub Contents API via Replit connector: split source/binary files, base64 encode, PUT with 100ms rate limit, skip >1MB files. Repo: skodabryansk-collab/debryansk-avto.
- [Auto track & sync skill](auto-track-and-sync.md) — Reusable skill for tracking changed files per session and syncing only those to GitHub via Contents API. Skip unchanged files, sequential upload, 409 auto-retry. Saved as `.agents/skills/auto-track-and-sync/SKILL.md`.
- [GitHub auto-sync policy](github-auto-sync-policy.md) — ALWAYS sync to GitHub after completing any task that modifies files. User expects this to happen automatically without being asked.
- [Auto.ru Dealer API](autoru-api.md) — catalog proxy: host apiauto.ru, auth header x-authorization (no Bearer/Vertis prefix), brands+models via /1.0/search/cars/breadcrumbs.
- [Calltouch REST API](calltouch-api.md) — правильный endpoint: calls-service/RestAPI/{siteId}/calls-diary/calls (не call-service/v1 и не calls-service/v1).
- [Phone normalization](phone-normalization.md) — normalizePhone/phoneHref util at src/lib/normalizePhone.ts. Digit[1]='9' → mobile 3-digit code (+7 (9XX) XXX-XX-XX), else landline 4-digit (+7 (XXXX) XX-XX-XX).
- [Навигатор AI chat](navigator-ai-chat.md) — JSON mode, gpt-5-mini, car catalog injected into prompt (50 used+60 new), returns {reply,car_ids[],action}. home.tsx is standalone (no Layout) — ChatWidget added directly. Empty reply fallback included.
- [Navigator DB & sync](navigator-db-sync.md) — cars table populated via syncCars() on startup+every 30min; owners_number parsed from Russian text ("Один владелец"→1); modification field included in catalog lines for KPP/drive context. Discount columns (max_discount, credit_discount, tradein_discount) added; ChatCarItem has minPrice (min per brand+model for new) and discount fields.
- [Fabric.js image loading](fabricjs-image-loading.md) — FabricImage.fromURL молча падает для storage-URL; паттерн: fetch→blob→HTMLImageElement→new FabricImage(el). Sharp: dynamic import внутри каждого handler.
- [Navigator chat persistence](navigator-chat-persistence.md) — messages persisted to localStorage key nav_messages_<session_id>; max 60 messages; cleared on decline.
- [GetLoyalty pagination wrap-around](getloyalty-pagination.md) — API paginates but wraps: pages 2+ repeat page 1 indefinitely. Must detect wrap-around (firstId in seenIds) and deduplicate rawList before DB insert. Client genuinely has ~6–10 reviews passing filter (4–5★, 90d, non-excluded).
- [VPS prerender setup](vps-prerender-setup.md) — Chrome stable deb (не snap); PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable в .env; PRERENDER_POOL_SIZE=1 на 961MB VPS; симлинк node_modules для spawnPrerender; FRONTEND_DIST_PATH нужен в index.ts startup.
- [Production routing — SSG fix](production-routing-ssg.md) — Replit prod routes "/" to debryansk-avto static CDN, not Express. Express middleware (prerender, seoMeta) never sees frontend requests. Fix: SSG script (scripts/ssg.mjs) generates dist/public/<route>/index.html at build time.
- [Home route prerender shell bug](home-prerender-shell.md) — dist/public/index.html is the SPA shell; loading it at startup blocks GCS from providing real Puppeteer HTML for "/". Fix: skip root index.html in startup SSG loader + pre-load GCS before spawning prerender.mjs.
- [GetLoyalty API](getloyalty-api.md) — POST /api/v2/reviews, Bearer token, response: {sources:{[key]:{platform,link}}, reviews:[{user.name, rate, source(key), date(unix)}]}.
- [Car sync outage guard](car-sync-outage-guard.md) — delete per-type (new/used separately); skip delete if feed returns 0 (outage guard). Promise.allSettled silently swallows feed errors — always log rejected results. Don't cache empty results from getNewCars. Debug endpoint: GET /api/debug/feeds. Force-sync: POST /api/admin/navigator/force-sync.
- [SEO audit progress](seo-audit-progress.md) — 7 раундов squirrelscan: 42→79/100. Что сделано, что осталось (thin content, author byline, orphan links, LCP preload), ключевые файлы и архитектура seoMeta.
- [Prerender-ready marker](prerender-ready-marker.md) — detail-страницы должны иметь `<div data-prerender-ready="true" style={{ display: "none" }} />` после загрузки данных; иначе Puppeteer timeout → пустой HTML в кэш.
- [JSON-LD array comparison bug](jsonld-array-comparison-bug.md) — `parsed["@type"] !== ["AutoDealer", "LocalBusiness"]` always true (JS array compare by ref). seoMeta stripped ALL ld+json from SSG. Fix: `Array.isArray(atType) && atType.includes("AutoDealer")`.
- [Brand page rules](brand-page-rules.md) — роут /brands/:slug, порядок секций, карта (map_x=lat/map_y=lng), новости по brand_id, HAVAL_CATALOG, как добавить новый бренд.
- [Brand description formula](brand-description-formula.md) — brand-page meta-description: официальный дилер/сервис + модели + мин. цена + MAX(max_discount) из таблицы cars; сервисные бренды без каталога получают сервисный шаблон. **Why:** чистая цена без выгоды звучит «по шаблону» и не отражает реального торгового предложения.
- [Used cars popularity](used-cars-popularity.md) — GET /api/cars/used joins cars DB (type='used') for popularity_score; POST /api/cars/views/used/:id increments; default sort "popular" in cars.tsx.
- [Production deployment build setup](production-deploy-build.md) — Vite build must be in the deployment build step; SSG reads index.html as template so stale SSG pages cause "text/html is not valid JS MIME type".
- [Shared queryKey mismatch](shared-query-key-mismatch.md) — все компоненты с одним queryKey должны иметь идентичный queryFn; иначе кэш содержит неверный тип → runtime crash при навигации.
- [Catalog refactor approach](catalog-refactor-approach.md) — CarCard+FilterPanel вынос + CarModelGroup + URL-sync — реализован, откатан (не понравился). DB-схема (car_views, popularity_score) уже создана.
- [News page SSG grid injection](news-ssg-grid.md) — /news не рендерит React карточки в статике (SPA shell 3162b). SSG now queries all article fields, calls buildNewsGridHtml(), and injects article cards into /news/index.html body before </body>. Fixes empty slug issue for Googlebot.
- [Prerender vs SSG SEO conflict](prerender-ssg-seo-conflict.md) — prerender middleware serving cached HTML directly bypassed seoMeta, silently dropping meta description, canonical, OG tags, and JSON-LD schema for ALL SSG pages. 3-layer defense: skip in prerender.mjs, fall through in middleware, reject in prerender-update endpoint.
- [Prerender brand page bugs](prerender-brand-page-bugs.md) — 4-bug chain: isSsgRoute+isSsgProtected blocked brands from cache; concurrent spawn OOM; data-prerender-ready set before data loads → 5KB shell. All 4 must be fixed together.
- [pg-pool crash loop + prerender DB guard](pg-pool-crash-guard.md) — pg-pool emits 'error' on idle connections during DB outage → uncaught exception → crash loop. Fix: pool.on('error', ...) in lib/db/src/index.ts. Also: startup prerender must check DB health first — running Puppeteer during outage overwrites GCS cache with broken pages.
- [Brands API cache for nav resilience](brands-api-cache.md) — /api/brands drives the nav menu; DB auth timeouts caused 41s wait → 500 → broken nav for all users. Fix: in-memory 5min TTL cache in public-brands.ts; serves last good response on DB error.
- [Prerender DB pool pressure](prerender-pool-pressure.md) — POOL_SIZE=5 in prerender.mjs × 3–4 API calls per page = ~15–20 concurrent DB queries; combined with user traffic causes pg auth timeouts. Keep POOL_SIZE=2 to prevent pool exhaustion during full crawls.
- [Prerender CSS hash staleness](prerender-css-hash-staleness.md) — GCS prerender cache for "/" can survive deploys with old CSS hash; fix: add "/" to SSG_ROUTES so seoMeta always reads live index.html.
- [Promotion slug pages](promo-slug-pages.md) — checklist for new slug-based prerendered detail routes (SSG guards, data-prerender-ready in every branch, reuse admin slugify pattern).
- [Prerender crawler meta deduplication](prerender-meta-deduplication.md) — prerender.mjs must send `x-prerender-bot: 1`; seoMeta skips server-side meta injection for non-SSG routes so React Helmet is the single source of truth. Prevents duplicate title/description/OG on /new-cars, /cars, /brands/*, car detail pages.
- [Soft 404 handling](soft-404-handling.md) — seoMeta returns HTTP 404 with `noindex, follow` for unknown routes when UA is a bot; browsers still get the SPA shell for client-side 404 rendering.
- [www to non-www redirect](www-redirect.md) — Express middleware at the top of app.ts redirects `www.debryansk-auto.ru` → canonical domain with 301, preserving path and query string.
- [КП tool — manager quotes](kp-manager-quotes.md) — esbuild copies kp-template.html+logo-da.svg to dist/; manager JWT shares ADMIN_JWT_SECRET; admin also passes requireManager (isAdmin===true).
- [Prerender car-sync stampede](prerender-car-sync-stampede.md) — car-sync updates=484 always true → spawn prerender every 30min → 484 Puppeteer×/api/cars/used → DB pool dead. Fix: spawn only on addedNewCarIds/addedUsedCarIds; deduplicate XML fetch in-flight.
- [Admin panel VPS deploy](admin-panel-vps-deploy.md) — nginx serves /admin from /opt/debryansk/admin/ (static). Must build with BASE_PATH=/admin/ NODE_ENV=production, then scp dist/public/. to VPS. Dev server changes are NOT visible on production.
- [OG image generator](og-image-generator.md) — Puppeteer 1200×630 PNG для /brands/:slug, /new-cars/:id, /cars/:id; 7-day disk cache /opt/debryansk/og-cache/; acquireChrome(1500) семафор; prerender middleware патчит og:image в кэшированном HTML через patchOgImage().
- [GCS→local disk migration gap](gcs-to-local-migration.md) — фото без .webp в UUID = старый GCS файл, не на VPS диске. Скачивать через @replit/object-storage Client, SCP в /opt/debryansk/uploads/uploads/.
- [Gemini image response format](gemini-image-response-format.md) — Gemini image models via /chat/completions return image in message.images[0].image_url.url (not content, which is null). GPT-image-2 → 500 for image input. /images/edits → 500. /v1/responses → 404.
- [Timeweb available models](timeweb-models.md) — gpt-4.1-mini НЕТ на Timeweb; используй gpt-4.1 / gpt-5-mini / gpt-5.4-mini. Оба AI_INTEGRATIONS_OPENAI_BASE_URL и OPENAI_BASE_URL на VPS = https://api.timeweb.ai/v1.
- [Vite admin-panel stale dist bug](vite-admin-dist-stale.md) — Vite сканирует dist/public/index.html как entry point; старый билд → "No matching export" на новые функции. Лечится: rm -rf dist/public && restart workflow.
- [SEO AI Autopilot](seo-ai-autopilot.md) — gpt-4.1 через Timeweb Gateway; apply-time generation; сигнатуры aiQueryToFaq/aiQueryToFaqCars/aiQueryToFaqModel/aiTextBlock/aiMetaDescription; DB: seo_ai_cache, seo_ai_examples, generated_by/reject_reason в seo_suggestions.
- [SEO GAP Engine](seo-gap-engine.md) — полная архитектура SEO-центра: 8 шагов GAP, Karpathy Loop, формула приоритета, типы предложений, BRAND_KEYWORDS vs site-wide coverage, правила доработки.
- [Prerender title sync from og:title](prerender-title-fix.md) — prerender.ts served cached HTML with wrong `<title>` (SPA default); fix: extract og:title from cached HTML and use it as `<title>` before res.send.
- [News SEO & gallery](news-seo-and-gallery.md) — stale JS hash на VPS news pages, deploy fix (rm news/ before upload), og:image/type/NewsArticle fixes, mobile gallery HeroGallery on all screens.
- [H1 injection for bots](seo-h1-injection.md) — SPA shell has no H1; inject via hidden <main> block in injectMeta(); regex /<h1 class="sr-only">/ never matched shell.
- [SEO center prod vs dev DB](seo-center-prod-db.md) — seo_suggestions/gap_runs/wordstat_snapshots are EMPTY in Replit dev DB; prod VPS has full data. Always query VPS via DATABASE_URL from /opt/debryansk/.env for real SEO center state.
- [Workflow restart port ownership](workflow-restart-port-ownership.md) — a stale API process can survive restart and keep serving old code on port 8080; verify listener ownership before testing.
- [VPS SSH transient refusal](vps-ssh-transient-refusal.md) — if Timeweb accepts one connection then refuses the next, use one ControlMaster session and a staged tar deploy.
- [Route health and safe prerender publication](route-health-integrity.md) — reconcile DB registry with every cache route; validate and atomically publish only crawl-safe snapshots.
- [Quote PDF priority](quote-pdf-priority.md) — интерактивная генерация КП должна прерывать длительный SEO-пререндер и не оставлять КП без файла.
- [Karpathy evaluator schedule](karpathy-evaluator-schedule.md) — оценка должна иметь ежедневный catch-up, а не зависеть только от воскресного снапшота позиций.
- [GAP pending stability](gap-pending-stability.md) — pending-гипотезы принадлежат менеджеру: GAP не создаёт и не перезаписывает их до решения.
- [Visitor conversion calls](visitor-conversion-calls.md) — completed >30 сек; звонки и заявки уникальны в rolling-окне 24 часа с учётом границы периода.
- [Non-interactive post-merge schema sync](post-merge-drizzle.md) — post-merge запускается без TTY; Drizzle push должен использовать non-interactive entry point.
- [GEO active hypothesis ownership](geo-active-hypothesis.md) — legacy applied rows without an evaluation schedule must not block new GEO recommendations forever.
- [Yandex Webmaster indexing samples](webmaster-indexing-samples.md) — v4 uses /indexing/samples, not /crawling/samples; read the tail because offset=0 can be historical.
- [Fuel type in stock feeds](fuel-type-feed.md) — CM Expert XML encodes diesel as a `d` suffix in modification_id; no dedicated fuel field currently exists.
- [VIN engine enrichment priority](vin-engine-enrichment-priority.md) — use `xml_pending` before CM lookup; only exact techParamId matches may become `cm_vin`, which feed sync must never overwrite.
- [SEO suggestion schema drift](seo-suggestion-schema-drift.md) — raw SQL fields need idempotent migrations in both dev and VPS, or the admin list can fail with 500.
